# دليل استخدام تطبيق SIHHA (شرح عربي كامل)

هذا الملف يشرح طريقة عمل التطبيق كما هو مطبق فعليًا في الكود الحالي (Flutter + Backend محلي Node.js + SQLite).

## 1) نظرة عامة سريعة

- التطبيق منصة استشارات طبية بين:
1. `مريض`
2. `طبيب`
3. `إدارة (Admin)`
- الواجهة: `Flutter`.
- الخادم: `Node.js + Express`.
- قاعدة البيانات: `SQLite`.
- ملفات الصور/الصوت تُحفظ محليًا داخل `backend/uploads`.
- لا يوجد اعتماد تشغيل فعلي على Firebase في النسخة الحالية.

## 2) تشغيل النظام

### تشغيل Backend

```bash
cd backend
npm install
copy .env.example .env
npm run dev
```

الخادم يعمل عادة على:
- `http://localhost:3000`

### تشغيل تطبيق Flutter

```bash
flutter pub get
flutter run --dart-define=API_BASE_URL=https://sihha.space/api
```

على جهاز حقيقي استبدل `10.0.2.2` بعنوان جهازك على الشبكة المحلية.

## 3) تسجيل الدخول وإنشاء الحساب

### حسابات المريض/الطبيب

- يمكن التسجيل من شاشة الدخول باختيار نوع الحساب (`مريض` أو `طبيب`).
- رقم الهاتف يُبنى بصيغة تشاد: `+235XXXXXXXX`.
- كلمة المرور:
1. عند إنشاء حساب جديد: 6 أحرف على الأقل.
2. عند تسجيل الدخول: الحد الأدنى في التحقق المحلي 4 أحرف.

### حساب الإدارة الافتراضي

الخادم ينشئ حساب إدارة افتراضي تلقائيًا إن لم يكن موجودًا:
- الرقم: `+23500000000`
- كلمة المرور الافتراضية: `0412`
- الاسم: `General Admin`

مهم: يفضّل تغيير كلمة مرور الإدارة فورًا.

## 4) رحلة الاستشارة (من المريض إلى الطبيب)

### الخطوة 1: الفرز الطبي الذكي قبل اختيار الطبيب

- من صفحة المريض الرئيسية:
1. الضغط على بطاقة `الفرز الطبي الذكي`.
2. اختيار نوع الأعراض الأساسية (عام / تنفسي / هضمي / نساء / أطفال / إصابة).
3. الإجابة على أسئلة `نعم/لا` حسب الحالة.
4. الضغط على `تحليل الحالة`.

نتيجة الفرز تعرض:
- درجة الخطورة: `منخفض` أو `متوسط` أو `مرتفع`.
- اقتراح التخصص المناسب:
1. `طبيب عام`
2. `طبيب أطفال`
3. `طبيب نساء`
4. `طوارئ`

في حالة الطوارئ:
- يظهر تنبيه واضح للمريض بأن الأفضل التوجّه الفوري للطوارئ.
- وعند محاولة بدء استشارة، يظهر تأكيد إضافي قبل المتابعة.

### الخطوة 2: المريض يختار طبيب

- بعد نتيجة الفرز يمكن:
1. تطبيق الاقتراح لتصفية قائمة الأطباء تلقائيًا.
2. البحث عن الطبيب بالاسم/الهاتف/التخصص.
3. التصفية اليدوية حسب التخصص الطبي.
4. الضغط على زر `استشارة`.

### الخطوة 3: تعبئة طلب الاستشارة

تظهر نافذة طلب فيها:
- المستفيد (`أنا` أو `شخص آخر`)
- اسم المريض/المستفيد
- العمر
- الجنس
- الوزن
- الولاية (تشاد)
- اللغة (عربي/فرنسي/مزدوج)
- الأعراض

التحقق الإجباري:
- الاسم: على الأقل حرفان.
- العمر: بين 0 و120.
- الوزن: بين 1 و400.
- الأعراض: على الأقل 5 أحرف.

### الخطوة 4: إرسال الطلب

بعد الإرسال:
- الطلب يُسجل بحالة `pending`.
- يظهر إشعار نجاح للمريض.

### الخطوة 5: كيف يستلم الطبيب الطلب

- الطبيب يرى الطلب في صندوق الوارد داخل شاشة الاستشارات.
- البطاقة تعرض:
1. اسم المريض
2. اسم المستفيد
3. العمر/الجنس/الوزن
4. الأعراض

### الخطوة 6: قرار الطبيب

الطبيب لديه 3 إجراءات:
1. `قبول`: يتم قبول الطلب، إنشاء/فتح غرفة محادثة وربطها بالطلب.
2. `رفض`: يتحول الطلب إلى `rejected`.
3. `تحويل`: تحويل الطلب لطبيب آخر عبر إدخال `doctorId`.

## 5) قواعد مهمة مرتبطة بطلبات الاستشارة

الخادم يمنع الحالات التالية:
- إنشاء طلب جديد إذا توجد غرفة استشارة مفتوحة مسبقًا مع نفس الطبيب.
- إنشاء طلب جديد إذا يوجد طلب `pending` مع نفس الطبيب.
- إنشاء طلب جديد إذا يوجد طلب نشط (pending/accepted) مع نفس الطبيب.

## 6) المحادثة داخل الاستشارة

أنواع الرسائل المدعومة:
- نصية
- صورة
- صوتية (عرض/تشغيل)
- أحداث Live خاصة بالمكالمة

المتوفر فعليًا في واجهة المحادثة الحالية:
- إرسال نص.
- إرسال صورة من المعرض.
- تشغيل الرسائل الصوتية إذا كانت موجودة.

ملاحظة مهمة: الكود الخلفي وطبقة المزود يدعمان إرسال الرسائل الصوتية، لكن واجهة الكتابة الحالية لا تعرض زر تسجيل صوت مباشر.

## 7) إغلاق الاستشارة

- إغلاق الاستشارة يتم بواسطة الطبيب فقط (زر القفل في شاشة المحادثة).
- عند الإغلاق:
1. الغرفة تُعلّم كمغلقة.
2. المريض لا يمكنه إرسال رسائل جديدة داخل هذه الغرفة.
3. يظهر للمريض زر `طلب استشارة جديدة` مع نفس الطبيب.

## 8) المكالمة الصوتية المباشرة (Live)

الآلية:
1. أحد الطرفين يرسل طلب مكالمة.
2. الطرف الآخر يستقبل تنبيه رنين مع خيارات قبول/رفض.
3. عند القبول يتم فتح شاشة مكالمة LiveKit الصوتية.

الشروط:
- يجب أن يكون الطرف الآخر نشطًا داخل المحادثة (Presence).
- إذا الطرف الآخر غير متصل: تظهر حالة `live-peer-offline`.
- يجب إعداد متغيرات LiveKit في `.env`:
1. `LIVEKIT_URL`
2. `LIVEKIT_API_KEY`
3. `LIVEKIT_API_SECRET`

داخل شاشة المكالمة:
- كتم/فتح المايك.
- تشغيل/إيقاف السماعة.
- إنهاء المكالمة.

## 9) بيانات الاستشارة داخل غرفة المحادثة

في أعلى شاشة المحادثة يظهر كرت بيانات الاستشارة:
- المريض
- المستفيد
- العمر/الجنس
- الوزن
- اللغة
- الأعراض

الطبيب يستطيع تعديل بيانات الاستشارة (إلا الطلبات المرفوضة).

## 10) السجل الطبي الإلكتروني (داخل غرفة الاستشارة)

لكل مريض يوجد سجل طبي مرتبط بالاستشارات، ويعرض:
- تاريخ الاستشارات السابقة (الطبيب + التاريخ + حالة مفتوحة/مغلقة).
- التشخيصات السابقة.
- الأدوية الموصوفة سابقًا.
- الحساسية.
- الأمراض المزمنة.

### صلاحيات الطبيب

الطبيب داخل نفس غرفة الاستشارة يستطيع:
1. تحديث بيانات السجل الطبي.
2. إضافة تشخيص جديد.
3. إضافة/تحديث الأدوية الموصوفة.
4. إضافة ملاحظات سرية للطبيب (لا تظهر للمريض).
5. رفع وصفة طبية بصيغة PDF وربطها بالسجل.

### صلاحيات المريض

- المريض يرى ملخص السجل الطبي العام (التاريخ، التشخيصات، الأدوية، الحساسية، الأمراض المزمنة).
- المريض لا يرى الملاحظات السرية الخاصة بالطبيب.

## 11) المدونات الصحية

### للمريض والطبيب

- تصفح كل المقالات.
- تصفية حسب التصنيف.

### للطبيب فقط

- إنشاء ونشر مقال جديد.
- الحقول المطلوبة: العنوان + التصنيف + المحتوى.
- شرط النشر: محتوى المقال 80 حرفًا على الأقل.

## 12) الحساب الشخصي والإعدادات

المتاح للمريض والطبيب:
- تغيير الصورة الشخصية من المعرض.
- تغيير كلمة المرور.
- تسجيل خروج.
- فتح شاشة الإعدادات.

شاشة الإعدادات:
- اللغة: `العربية` أو `الفرنسية`.
- الثيم: `فاتح` أو `داكن` أو `حسب الجهاز`.

إضافات للطبيب:
- تعديل التخصص.
- تعديل اسم المستشفى.
- تعديل سنوات الخبرة والدراسة.

## 13) لوحة الإدارة (Admin)

لوحة الإدارة تحتوي 3 تبويبات:
1. `الإحصائيات`
2. `إنشاء حساب`
3. `إدارة الحسابات`

### الإحصائيات

- إجمالي المستخدمين.
- عدد الأطباء والمرضى.
- الحسابات المعطلة.
- الزوار: اليوم/الشهر/السنة.
- المتصلون الآن.
- أداء الأطباء (عدد المرضى الذين تمت معاينتهم يوميًا/شهريًا/سنويًا).

### إنشاء حساب

- إنشاء مريض أو طبيب.
- عند اختيار طبيب يمكن إدخال:
1. التخصص
2. المستشفى
3. سنوات الخبرة
4. سنوات الدراسة

### إدارة الحسابات

لكل حساب (عدا الإدارة الأساسية):
- تفعيل/تعطيل
- إعادة تعيين كلمة المرور
- حذف الحساب

قيود حماية:
- لا يمكن تعطيل حساب الإدارة الرئيسية.
- لا يمكن حذف حساب الإدارة الرئيسية.
- لا يمكن للإداري تعطيل نفسه أو حذف نفسه.

## 14) قيود وقواعد تشغيل مهمة

- أرقام الهاتف مبنية على +235.
- رقم `00000000` محجوز لحساب الإدارة.
- أدوار التطبيق الأساسية: `patient` و`doctor` (مع تمييز إداري عبر رقم الإدارة).
- رفع الملفات:
1. صورة بروفايل: `uploads/profile_photos/...`
2. صور المحادثة: `uploads/chat_images/...`
3. صوتيات المحادثة: `uploads/chat_audio/...`

## 15) ملخص سيناريو عملي سريع

### كيف يجري المريض استشارة؟

1. يسجل الدخول كمريض.
2. ينفّذ الفرز الطبي الذكي (اختيار نوع الحالة + الإجابة على الأسئلة).
3. يراجع درجة الخطورة واقتراح التخصص.
4. يختار الطبيب المناسب (أو يطبق اقتراح التخصص).
5. يملأ نموذج الاستشارة.
6. يرسل الطلب.
7. ينتظر قبول الطبيب.
8. بعد القبول يدخل غرفة المحادثة ويراسل الطبيب.

### كيف يستلم الطبيب الطلب؟

1. يسجل الدخول كطبيب.
2. يفتح تبويب الاستشارات.
3. يجد الطلب في صندوق الطلبات الواردة.
4. يراجع البيانات.
5. يختار قبول/رفض/تحويل.
6. عند القبول تُفتح المحادثة مباشرة مع بيانات الحالة.

### ماذا تستطيع الإدارة؟

1. متابعة الإحصائيات والزوار.
2. إنشاء الحسابات.
3. إدارة وتعطيل/تفعيل الحسابات.
4. إعادة ضبط كلمات المرور.
5. حذف الحسابات (مع القيود المذكورة).

