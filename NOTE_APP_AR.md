# دليل استخدام تطبيق SIHHA (محدّث للكود الحالي)

هذا الملف يشرح طريقة عمل التطبيق كما هو مطبّق فعليًا في الكود الحالي (`Flutter` + `Node.js/Express` + `PostgreSQL`).

## 1) نظرة عامة سريعة

- التطبيق منصة استشارات طبية بين:
1. `مريض`
2. `طبيب`
3. `إدارة (Admin)`
- الواجهة: `Flutter`.
- الخادم: `Node.js + Express`.
- قاعدة البيانات: `PostgreSQL`.
- ملفات الصور/الصوت/PDF تُحفظ داخل `backend/uploads`.
- Firebase غير مستخدم في مسار التشغيل الحالي.

## 2) تشغيل النظام

### تشغيل Backend

```bash
cd backend
npm install
copy .env.example .env
npm run dev
```

الخادم يعمل عادة على:
- `http://localhost:3000`

مهم قبل التشغيل:
- يجب ضبط `DATABASE_URL` في `.env` (أو متغيرات `PGHOST/PGUSER/PGDATABASE`).
- للمكالمة الصوتية: إعداد متغيرات `LIVEKIT_URL`, `LIVEKIT_API_KEY`, `LIVEKIT_API_SECRET`.
- لميزة Triage الذكية على مستوى الـ API: إعداد `OPENAI_API_KEY`.

### تشغيل Flutter

```bash
flutter pub get
flutter run --dart-define=API_BASE_URL=https://sihha.space/api
```

مثال على جهاز حقيقي (اتصال محلي):

```bash
flutter run --dart-define=API_BASE_URL=http://YOUR_PC_IP:3000/api
```

ملاحظة:
- القيمة الافتراضية داخل التطبيق هي `https://sihha.space/api` إذا لم تمرر `API_BASE_URL`.

## 3) تسجيل الدخول وإنشاء الحساب

### حسابات المريض/الطبيب

- التسجيل من شاشة الدخول باختيار نوع الحساب (`مريض` أو `طبيب`).
- الهاتف بصيغة تشاد: `+235XXXXXXXX`.
- كلمة المرور:
1. إنشاء حساب جديد: `6` أحرف على الأقل.
2. التحقق المحلي في شاشة الدخول: `4` أحرف على الأقل.
3. تغيير كلمة المرور من الإعدادات: `8` أحرف على الأقل (تحقق الخادم).

### حساب الإدارة الافتراضي

الخادم ينشئ حساب إدارة افتراضي تلقائيًا إن لم يكن موجودًا:
- الرقم: `+23500000000`
- كلمة المرور الافتراضية: `0412`
- الاسم: `General Admin`

مهم:
- الرقم `00000000` (محليًا) محجوز للإدارة.
- يفضّل تغيير كلمة مرور الإدارة فورًا.

## 4) رحلة المريض في النسخة الحالية

### الخطوة 1: اختيار الطبيب

من الصفحة الرئيسية للمريض:
- البحث عن الطبيب بالاسم/الهاتف/التخصص.
- تصفية حسب التخصص الطبي.
- الضغط على `استشر الآن`.

إذا كانت هناك غرفة استشارة قائمة وغير مغلقة مع نفس الطبيب:
- يتم فتح الغرفة مباشرة بدل إنشاء طلب جديد.

### الخطوة 2: تعبئة طلب الاستشارة

تظهر نافذة طلب تشمل:
- المستفيد (`أنا` أو `شخص آخر`)
- اسم المستفيد
- العمر
- الجنس
- حالة الحمل (عند اختيار `أنثى`)
- الوزن
- الولاية (حسب قائمة ولايات تشاد)
- اللغة (`عربي` / `فرنسي` / `مزدوج`)
- الأعراض (اختيار أعراض + ملاحظات نصية)
- تسجيل صوتي للأعراض (اختياري) مع رفعه

### التحقق الإجباري

- الاسم: على الأقل حرفان.
- العمر: عدد صحيح بين `0` و`120`.
- الوزن: بين `1` و`400`.
- الأعراض: نص صالح بطول مناسب (والواجهة تضمن تكوين وصف صالح قبل الإرسال).

### الخطوة 3: إرسال الطلب

بعد الإرسال:
- الطلب يُسجل بحالة `pending`.
- يظهر إشعار نجاح للمريض.

## 5) رحلة الطبيب مع طلبات الاستشارة

من شاشة الاستشارات:
- الطبيب يرى صندوق طلبات واردة.
- لكل طلب يمكن تنفيذ:
1. `قبول`
2. `رفض`
3. `تحويل` لطبيب آخر (بإدخال `doctorId`)

عند القبول:
- يتم إنشاء/فتح غرفة محادثة وربطها بالطلب.

## 6) قواعد مهمة لمنع التكرار

الخادم يمنع إنشاء طلب جديد مع نفس الطبيب إذا كان هناك:
- غرفة استشارة مفتوحة.
- طلب `pending`.
- طلب نشط (`pending` أو `accepted`).

## 7) المحادثة داخل الاستشارة

المدعوم في النظام:
- رسائل نصية.
- رسائل صور.
- رسائل صوتية (مدعومة Backend/Provider ويمكن تشغيلها).
- رسائل/أحداث Live للمكالمة.

المتوفر فعليًا في واجهة الكتابة الحالية:
- إرسال نص.
- إرسال صورة.
- بدء مكالمة صوتية Live.

ملاحظة:
- لا يوجد زر تسجيل صوت مباشر داخل شريط كتابة المحادثة حاليًا.

## 8) إغلاق الاستشارة

- الإغلاق بواسطة الطبيب فقط.
- عند الإغلاق:
1. الغرفة تُعلّم كمغلقة.
2. المريض لا يستطيع إرسال رسائل جديدة داخلها.
3. تظهر رسالة حالة إغلاق داخل الشاشة.

## 9) المكالمة الصوتية المباشرة (LiveKit)

الآلية:
1. إرسال طلب مكالمة من أحد الطرفين.
2. الطرف الآخر يقبل أو يرفض.
3. عند القبول يتم الانضمام لمكالمة LiveKit الصوتية.

الشروط:
- الطرفان يجب أن يكونا نشطين داخل المحادثة (Presence).
- إذا الطرف الآخر غير متصل تظهر أخطاء مثل `live-peer-offline`.

داخل شاشة المكالمة:
- كتم/فتح المايك.
- تشغيل/إيقاف السماعة.
- إنهاء المكالمة.

## 10) المدونات الصحية

### للمريض والطبيب

- تصفح المقالات.
- التصفية حسب التصنيف.
- التفاعل مع المقالات (`like/support/thanks`).
- التعليقات (قراءة/إضافة).
- المشاركة (Facebook / X / WhatsApp / Telegram) مع تسجيل إحصائية المشاركة.

### للطبيب فقط

- إنشاء ونشر مقال جديد.
- الحقول الأساسية: العنوان + التصنيف + المحتوى.
- شرط النشر: المحتوى `80` حرفًا على الأقل.
- يمكن رفع حتى 3 صور تعبيرية للمقال.

## 11) الحساب الشخصي والإعدادات

المتاح للمريض والطبيب:
- تحديث الصورة الشخصية (كاميرا أو معرض).
- تغيير كلمة المرور.
- تسجيل خروج.
- تغيير اللغة (`العربية` / `الفرنسية`).
- تغيير الثيم (`فاتح` / `داكن` / `حسب الجهاز`).

## 12) لوحة الإدارة (Admin)

تحتوي 3 تبويبات:
1. `الإحصائيات`
2. `إنشاء حساب`
3. `إدارة الحسابات`

### الإحصائيات

- إجمالي المستخدمين.
- عدد الأطباء والمرضى.
- الحسابات المعطلة.
- الزوار: اليوم/الشهر/السنة.
- المتصلون الآن.
- أداء الأطباء (يومي/شهري/سنوي).

### إنشاء حساب

- إنشاء مريض أو طبيب.
- عند الطبيب يمكن إدخال: التخصص، المستشفى، سنوات الخبرة، سنوات الدراسة.

### إدارة الحسابات

لكل حساب (عدا الإدارة الرئيسية):
- تفعيل/تعطيل.
- إعادة تعيين كلمة المرور.
- حذف الحساب.

قيود حماية:
- لا يمكن تعطيل أو حذف الإدارة الرئيسية.
- لا يمكن للإداري تعطيل نفسه أو حذف نفسه.

## 13) التخزين والملفات

المسارات الأساسية داخل `backend/uploads`:
1. صورة البروفايل: `uploads/profile_photos/...`
2. صور المحادثة/المدونات: `uploads/chat_images/...`
3. صوت المحادثة/الأعراض: `uploads/chat_audio/...`
4. وصفات PDF: `uploads/prescriptions/...`

## 14) ملاحظات مطابقة مهمة للكود الحالي

- قاعدة البيانات الحالية هي `PostgreSQL` (مع سكربت ترحيل من SQLite: `npm run migrate:sqlite:pg`).
- يوجد endpoint للفرز الذكي في الخادم: `POST /api/triage/analyze` ويعتمد على OpenAI.
- واجهة Flutter الحالية لا تستخدم endpoint الترياج الذكي مباشرة في المسار الرئيسي.
- توجد طبقة بيانات كاملة للسجل الطبي في الخادم/المزود، لكنها غير معروضة كشاشة مستقلة مكتملة في الواجهة الحالية.

## 15) ملخص عملي سريع

1. المريض يختار طبيبًا من القائمة.
2. يملأ طلب الاستشارة (مع خيار صوت للأعراض).
3. الطبيب يستقبل الطلب ويقبل/يرفض/يحوّل.
4. عند القبول تُفتح غرفة المحادثة.
5. تتم المتابعة نصيًا/صورًا مع إمكانية مكالمة صوتية Live.
6. الطبيب يغلق الاستشارة عند الانتهاء.
